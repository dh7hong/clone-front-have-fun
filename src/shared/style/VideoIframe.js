import styled from 'styled-components';

import Iframe from "react-iframe";

export const VideoIframe = styled(Iframe)`
  width: 100%; // Full width of the grid item
  height: 200px; // Fixed height for each video
`;